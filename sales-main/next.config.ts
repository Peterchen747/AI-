import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["desktop-d0m2r10.tailddf901.ts.net"],
  serverExternalPackages: ["@react-pdf/renderer"],
};

export default nextConfig;
